---
- name: Setup repositories using oVirt release package
  hosts: virthosts

  vars:
    ovirt_repositories_ovirt_release_rpm: https://resources.ovirt.org/pub/yum-repo/ovirt-release42.rpm

  roles:
    - role: oVirt.repositories
